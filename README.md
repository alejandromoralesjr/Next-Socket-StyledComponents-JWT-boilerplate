# Next / Socket.io / Styled Components / JWT boilerplate

## Packages installed

- Next
- Socket.io
- Styled-components
- JSON Web Token

```bash
# Clone project, replace PROJECT_NAME
git clone git@github.com:alejandromoralesjr/
Next-Socket-StyledComponents-JWT-boilerplate.git PROJECT_NAME
# Install packages
npm i
```
